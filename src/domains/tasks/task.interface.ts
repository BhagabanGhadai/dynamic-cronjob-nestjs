export enum ECurrentStatus {
    active = 'active',
    inactive = 'inactive',
    delete = 'delete'
}

export enum ETaskType {
    cron = 'cron',
    interval = 'interval',
    timeout = 'timeout'
}

export enum ETimeZone {
    IN = 'Asia/Kolkata',
    US = 'America/New_York',
    UK = 'Europe/London',
}